<div class="row">
  <div class="col-lg-5 col-md-3 d-none d-md-block mt-auto mb-auto text-center">
    <h4>Invite ton entourage pour comparer vos empreintes carbones en quelques minutes !</h4>
    <p>Tu seras prévenu par email dès que son profil est prêt pour votre match.</p>
    <div class="signup">
      <%= image_tag "invitation.png", class: "img-fluid", style: ""%>
    </div>
  </div>
  <div class="col-lg-5 col-md-6">
    <div class="card">
      <div class="card-header card-header-dark text-center">
        <h3 class="card-title">Inviter un utilisateur par email</h3>
      </div>
      <%= form_for(resource, as: resource_name, url: invitation_path(resource_name), html: { method: :post }) do |f| %>
        <div class="authform">
          <div class="card-body">
            <%= render "devise/shared/error_messages", resource: resource %>
            <% resource.class.invite_key_fields.each do |field| -%>
              <div class="form-group bmd-form-group">
                <%= f.label field, class: 'bmd-label-floating' %>
                <%= f.email_field field, :autofocus => true, class: 'form-control' %>
              </div>
            <% end %>
          </div>
          <div class="card-footer">
            <%= f.submit "Envoyer l'invitation", :class => 'btn btn-primary btn-lg btn-round mr-auto ml-auto'%>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
