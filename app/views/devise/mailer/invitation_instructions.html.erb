<h1><%= User.find(@resource.invited_by_id).name || User.find(@resource.invited_by_id).email %> t'invite à comparer votre empreinte carbone sur Carbo !</h1>
<p>Bonjour <%= @resource.name || @resource.email %> 👋,</p>
<p>
  <%= User.find(@resource.invited_by_id).name || User.find(@resource.invited_by_id).email %> utilise <%= link_to "Carbo", accept_invitation_url(@resource, invitation_token: @token, utm_source: 'invitation_email', utm_medium: 'email') %> pour mieux comprendre, réduire et compenser son empreinte carbone.
</p>
<p>
   Son message est clair : <%= User.find(@resource.invited_by_id).name || User.find(@resource.invited_by_id).email %> pense faire mieux que toi ! Heureusement, 5 petites minutes te suffiront à rejoindre gratuitement la communauté Carbo et lui montrer qui est le meilleur ✊ Mais, au fait...es-tu le meilleur ?
</p>
<%= link_to "RELEVER LE DÉFI DE #{User.find(@resource.invited_by_id).name || User.find(@resource.invited_by_id).email}", accept_invitation_url(@resource, invitation_token: @token, utm_source: 'invitation_email', utm_medium: 'email'), class: "btn btn-round btn-primary" %>
<p>À très vite ✌</p>
<p>Emmanuel<br>
Cofondateur de Carbo</p>
