<h1><%= User.find(@resource.invited_by_id).name || User.find(@resource.invited_by_id).email %> t'invite à comparer votre empreinte carbone sur Carbo !</h1>
<p>Bonjour <%= @resource.email %>,</p>
<p>
  <%= User.find(@resource.invited_by_id).name || User.find(@resource.invited_by_id).email %> s'est inscrit sur Carbo pour suivre, comparer, réduire et compenser son empreinte carbone facilement.
</p>
<p>
   En acceptant cette invitation, tu pourras t'inscrire à ton tour et comparer ton empreinte carbone avec la sienne en moins de 10 minutes.
</p>
<%= link_to "Accepter l'invitation", accept_invitation_url(@resource, invitation_token: @token, utm_source: 'invitation_email', utm_medium: 'referal'), class: "btn btn-round btn-primary" %>
<p>À très vite 👋</p>
<p>Emmanuel<br>
Cofondateur de Carbo</p>
