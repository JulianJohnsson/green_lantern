<nav id="app-navbar" class="navbar navbar-expand-lg navbar-transparent navbar-absolute fixed-top">
  <div class="container-fluid">
    <div class="navbar-wrapper">
      <p class="navbar-brand">
        <%= content_for?(:title) ? yield(:title) : "Carbo" %>
      </p>
    </div>
    <button class="navbar-toggler" type="button" data-toggle="collapse" aria-controls="navigation-index" aria-expanded="false" aria-label="Toggle navigation">
      <span class="sr-only">Toggle navigation</span>
      <span class="navbar-toggler-icon icon-bar"></span>
      <span class="navbar-toggler-icon icon-bar"></span>
      <span class="navbar-toggler-icon icon-bar"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end">
      <ul class="navbar-nav">
      <% if current_user.transactions.category_id(115).count > 0 %>
        <li class="nav-item dropdown">
          <a class="nav-link" href="/categorize" data-toggle="tooltip" data-placement="bottom" title="" data-container="body" data-original-title="Pour améliorer la précision de ton score, précise la catégorie de
              <%= current_user.transactions.category_id(115).count %> dépenses.">
            <i class="material-icons">notifications</i>
            <span class="notification">
              <%= current_user.transactions.category_id(115).count %>
            </span>
            <div class="ripple-container"></div>
          </a>
        </li>
      <% end %>
        <li class="nav-item dropdown">
          <a class="nav-link" href="" id="navbarDropdownProfile" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <p><%= current_user.name %>
            <i class="material-icons">person</i></p>
          </a>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownProfile">
            <% if user_signed_in? %>
              <%= link_to '⚙ Paramètres', edit_user_registration_path, class: 'dropdown-item' %>
              <%= link_to '📋 Mode de vie', edit_score_path(current_user.scores.last), class: 'dropdown-item' %>
              <%= link_to '🚀 Progression', '/badges', class: 'dropdown-item' %>
              <% if current_user.scores.last.kind.to_sym == :dynamic %>
                <%= link_to '🔃 Comptes synchronisés', user_path(current_user), class: 'dropdown-item' %>
              <% end %>
              <%= link_to '🍄 Aide', "https://www.hellocarbo.com/?page_id=199", class: 'dropdown-item', :target=>'_blank' %>
              <div class="dropdown-divider"></div>
              <%= link_to '⭕ Déconnexion', destroy_user_session_path, :method=>'delete', class: 'dropdown-item' %>
            <% else %>
              <%= link_to 'Inscription', new_user_registration_path, class: 'btn btn-info btn-raised btn-round' %></li>
            <% end %>
          </div>
        </li>
      </ul>
    </div>
  </div>
</nav>
