<!DOCTYPE html>
<html lang="fr">
  <head>
    <link rel="icon" type="image/png" sizes="96x96" href="/favicon-96x96.png">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title><%= content_for?(:title) ? yield(:title) : "Carbo" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "L’impact de tes choix quotidiens. Carbo, l'application qui calcule automatiquement le poids carbone de toutes tes dépenses bancaires." %>">
    <%= stylesheet_link_tag "https://fonts.googleapis.com/css?family=Material+Icons|Baloo|Maven+Pro:400,700,900|Quicksand:400,700&display=swap" %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div class="wrapper">
      <% if (Bridge.find_by_user_id(current_user.id) == nil || Bridge.find_by_user_id(current_user.id).bank_connected == false) %>
        <%= render '/layouts/application_sidebar_trump' %>
        <%= render 'layouts/push_bridge' %>
      <% else %>
        <%= render 'layouts/application_sidebar' %>
      <% end %>
      <div class="main-panel" style="<%= 'margin-top:60px' if (Bridge.find_by_user_id(current_user.id) == nil || Bridge.find_by_user_id(current_user.id).bank_connected == false) %>">
        <%= render 'layouts/application_navbar' %>
        <div class="content">
          <div class="container-fluid">
            <%= render 'layouts/messages' %>
            <%= yield %>

          </div>
        </div>
        <%= render 'layouts/footer' %>
      </div>
    </div>
    <%= render 'layouts/messenger' %>
  </body>
  <%= render 'layouts/analytics' %>
  <script type="text/javascript">
  $(document).ready(function(){
    setTimeout(function(){
    $('.alert').fadeOut();
    }, 3000);
  })
  </script>
</html>
