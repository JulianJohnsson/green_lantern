<p id="notice"><%= notice %></p>

<h1>Bridges</h1>

<table>
  <thead>
    <tr>
      <th>Token</th>
      <th>Expire at</th>
      <th>Bank connected</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bridges.each do |bridge| %>
      <tr>
        <td><%= bridge.token %></td>
        <td><%= bridge.expire_at %></td>
        <td><%= bridge.bank_connected %></td>
        <td><%= bridge.user %></td>
        <td><%= link_to 'Show', bridge %></td>
        <td><%= link_to 'Edit', edit_bridge_path(bridge) %></td>
        <td><%= link_to 'Destroy', bridge, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Bridge', new_bridge_path %>
